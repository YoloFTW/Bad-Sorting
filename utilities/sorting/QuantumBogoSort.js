const { IsSorted } = require("../IsSorted")

module.exports = {

    /**
     * Quantum Bogo Sorts the array
     * @param {Array} arr array to sort
     * @returns {Array} 
     */
    QuantumBogoSort: (arr) => {

        if (IsSorted(arr)) {
            return arr;
        
        }else{

            //TODO: Impliment destruction of universe

        }

    }

}